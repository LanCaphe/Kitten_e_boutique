<tbody>
      <td><%= @item.title %></td>
      <td><%= @item.description %></td>
      <td><%= @item.price %></td>
      <td><%= image_tag @item.image_url %></td>
      <td><%= link_to 'Add to cart', add_item_url(@item.id), :class => "shop__btn shop__btn__cart" %></td>
      <td><%# link_to 'Add to cart', add_item_url(@item.id), remote: true %></td>
</tbody>
